# 项目 Review

适用：代码接手人、现场联调人员、产品/项目负责人。

---

## 一、优先处理的问题

### P0: MQTT 远程 shell 执行

`mqtt_ros2_bridge.py` 收到 `topic_command` 后直接 `Popen(shell=True)` 执行本机命令，结果回传 MQTT。只要凭据泄露即可远程控制机器人。

建议：移除或加命令白名单+鉴权，生产环境默认关闭。

### P1: 硬编码路径/端口

串口、文件路径、MQTT 参数直接写在 `run.launch.py` 和代码中。当前映射：

| 设备 | 当前端口 |
|------|----------|
| RTK 接收机 (WTRTK) | `/dev/ttyS2` |
| 485 传感器 | `/dev/ttyS1` |
| 激光测距 | `/dev/laser` |
| CAN 总线 | `can0` (socketcan) |

换机器需逐一修改。建议改为 launch 参数或 yaml 配置。

### P2: 配置与逻辑耦合

充电阈值、清扫流程、进仓参数等业务参数混在代码中。建议抽出部署配置层和行为策略配置层。

---

## 二、已有能力确认

- 差速底盘运动控制（键盘/遥控/RTK 三模）
- RTK 多航点导航 + Stanley 横向控制
- 路径文件加载与跨文件自动切换
- 激光辅助进仓对位（含电机/激光异常兜底）
- 出仓、进仓完整状态机
- 电池/电流/电压/温度采集
- 485 充电模块启停与故障查询
- MQTT 指令桥接与状态上报
- 区域路径规划（YAML 配置 + 稠密拼接）

---

## 三、需确认的需求

1. **控制权优先级**：遥控/键盘/MQTT/RTK 谁优先，抢占后是否自动恢复
2. **RTK 导航判定**：到点阈值、航向误差允许范围、非固定解策略、丢星时是否靠 IMU
3. **边界触发策略**：6 路 IO 各代表什么位置、触边后是避让还是绕障
4. **进仓完成条件**：按激光/距离/充电接触判定，失败重试机制
5. **充电策略**：自动充电前提、满充判定、恢复充电阈值
6. **路径规划规范**：使用单区域矩形还是三点标定多区域、路径文件谁维护
7. **MQTT 职责边界**：业务命令 vs 运维命令、是否需要远程终端

---

## 四、快速排查顺序

现场出问题按此顺序排查：

1. **设备层**：串口/CAN/485/激光/RTK 是否在位
2. **launch 配置**：端口、路径文件、MQTT 参数是否匹配当前机器
3. **话题流**：`/fix`、`/wtrtk_data`、`/laser_distance` 是否有数据
4. **控制模式**：当前在 NORMAL/REMOTE/AUTO_CLEANING/HOLD/DISABLE
5. **算法逻辑**：RTK 到点、Stanley 纠偏、边界、进仓对位

---

## 五、建议补齐的文档

- `README.md`：启动说明和整体流程
- `TOPICS.md`：所有 ROS topic/service/parameter 列表
- `DEPLOY.md`：串口映射、驱动、systemd 部署步骤
- `REQUIREMENTS.md`：控制权、回仓、充电、异常处理规则

---

## 六、结论

主体功能完整，当前最大阻碍：
- 运维与安全边界不清
- 配置硬编码较多
- 文档和需求边界不够清晰

优先：文档 & 配置入口 → 控制权/回仓/充电需求确认 → 算法细节优化
